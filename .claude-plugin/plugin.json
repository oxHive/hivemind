{
  "name": "hivemind",
  "version": "0.8.0",
  "description": "HiveMind: persistent memory for AI coding agents. Gives Claude Code access to a local memory store that survives across sessions.",
  "commands": ["./plugins/claude/skills"],
  "mcpServers": {
    "hivemind": {
      "command": "hivemind",
      "args": []
    }
  }
}
